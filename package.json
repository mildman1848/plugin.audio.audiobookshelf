{
  "name": "plugin.audio.audiobookshelf",
  "version": "0.1.43",
  "private": true,
  "description": "Tooling configuration for the plugin.audio.audiobookshelf repository",
  "scripts": {
    "build": "python scripts/build_release.py",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint:python": "python scripts/validate_repo.py",
    "lint:xml": "python scripts/validate_repo.py",
    "validate": "npm run lint:python && npm run lint:xml && npm run build"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
